import 'package:flutter/material.dart';

class HomeOverlayWidget extends StatelessWidget {
  const HomeOverlayWidget({super.key});

  @override
  Widget build(context) {
    return Container(
      color: Colors.red,
    );
  }
}
